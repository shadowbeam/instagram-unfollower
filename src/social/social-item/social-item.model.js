export default class SocialItem {

    icon: string;
    url: string;
    hex: string;

    constructor(icon: string, url:string, hex:string) {
        this.icon = icon;
        this.url = url;
        this.hex = hex;
    }

}
